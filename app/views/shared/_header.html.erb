<nav class="navbar navbar-expand-lg navigation">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="container-fluid navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-between">

        <%# if the page is not the home page it must lead to the previous page with a back arrow  %>
        <% if title != 'Groups' && title != 'Add New Purchases' %>
          <li class="nav-item">
            <%= link_to groups_path(@group) do %>
              <button class="btn btn-outline-light" type="button" onclick="history.back()">
                <i class="bi bi-arrow-left"></i>
              </button>
            <% end %>
          </li>
        <% end %>
        <% if title == 'Add New Purchases' %>
          <li class="nav-item">
          <%= link_to group_purchases_path(group_id: @group.id) do %>
            <button class="btn btn-outline-light" type="button">
              <i class="bi bi-arrow-left"></i>
            </button>
          <% end %>
          </li>
        <% end %>

        <li class="nav-item fs-4 fw-bold">
          <%= title %>
        </li>

        <li class="nav-item">
          <button class="btn btn-outline-light" type="button">
            <i class="bi bi-box-arrow-right"></i>
          </button>
        </li>
      </ul>
    </div>
  </div>
</nav>